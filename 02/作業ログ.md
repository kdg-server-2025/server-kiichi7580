## 1回目の授業
```bash
# sshコマンドで認証が通るかを確認
ssh -T git@github.com

# goのパスを通す
export PATH=$PATH:/usr/local/go/bin
source ~/.zshrc

# goファイルのプログラムの実行
go run ファイル名

# go-hello という名前でイメージをビルド
docker build -t go-hello .
docker run go-hello
```